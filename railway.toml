# Railway deployment config
# This file tells Railway how to run the app

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
